// Mirrors --icon-size-* and layout custom properties in styles/foundation/_tokens.scss.
export const iconSize = {
  xxs: 10,
  xs: 14,
  sm: 18,
  md: 20,
  lg: 28,
};

export const serviceIconSize = {
  sm: 32,
  md: 36,
  lg: 48,
};

export const layoutSize = {
  minWidth: 'var(--layout-min-width)',
  maxWidth: 'var(--layout-max-width)',
  wideWidth: 'var(--layout-wide-width)',
  headerHeight: 'var(--header-height)',
  headerBarHeight: 'var(--header-bar-height)',
  headerNavHeight: 'var(--header-nav-height)',
  bottomTabHeight: 'var(--bottom-tab-height)',
  drawerWidth: 'var(--drawer-width)',
};
